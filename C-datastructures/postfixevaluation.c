#include <stdio.h>
#include <stdlib.h>

stuct node {
  int data;
  struct node *top;
}
*h

    int item;
struct node *new;
new = (struct node *)malloc(sizeof(struct node *));
printf("enter the data: ");
